<%- include("header") -%>
                  <div class="dropdown" id="myDiv">
                    <button id="bttn" class="dropbtn">Top Gem</button>
                    <div id="insert" class="dropdown-content">
<a id="first"><%= gem1 %></a>
                    </div>
                  </div>

                </li>
                    </ul>
                </div>
                </nav>
                <div class="my-3 p-3 bg-white rounded shadow-sm">
                <h6 class="border-bottom border-gray pb-2 mb-0">Downloads</h6>

              <% docs.forEach(function(item){ %>

                  <div class="media text-muted pt-3">

                   <i class="fa fa-file-pdf-o fa-2x favi"></i>
                  <div class="media-body pb-3 mb-0 small lh-125 ">
                     <div class="d-flex justify-content-between align-items-center w-100">
                       <strong class="text-gray-dark"><%=item.name%></strong>
                        <div class="downloads">Total Downloads: <%=item.count %>
                  <a download href="./downloads/<%=item.course%>/<%=item.name%>"><i class="fa fa-download fa-2x downButton" style="color:#000"></i></a>
                        </div>

                     </div>
                     <span class="d-block">Date</span>

                   </div>
                  </div>
              <%  }) %>


                </div>
      <%- include("footer") -%>
